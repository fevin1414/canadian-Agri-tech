<% if flash[:notice] %>
  <p class="notice"><%= flash[:notice] %></p>
<% end %>

<h1>Edit Category</h1>

<%= form_with(model: @category, url: admin_category_path(@category), method: :patch) do |form| %>

    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit 'Update Category' %>
  </div>
<% end %>

<%= link_to 'Back to Dashboard', admin_dashboard_path%>
