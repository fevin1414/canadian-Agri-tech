<h1>Users</h1>
<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td>
          <%= link_to 'View', admin_show_user_path(user) %>
          <%= link_to 'Remove', admin_remove_user_path(user), method: :delete, data: { confirm: 'Are you sure you want to remove this user?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
